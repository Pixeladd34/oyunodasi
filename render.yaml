services:
  - type: web
    name: air-console-server
    env: node
    rootDir: server
    buildCommand: npm install && npm run build
    startCommand: npm run start
    plan: free
    autoDeploy: true

